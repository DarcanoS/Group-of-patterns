# Air Quality Platform - Database Containers Configuration
# Copy this file to .env and customize with your values

# ============================================================================
# PostgreSQL + PostGIS Configuration
# ============================================================================

# PostgreSQL superuser password (change in production!)
POSTGRES_PASSWORD=postgres_secure_pass

# Port mapping (5433 to avoid conflicts with other PostgreSQL instances)
POSTGRES_PORT=5433

# ============================================================================
# MongoDB Configuration
# ============================================================================

# MongoDB root credentials (change in production!)
MONGO_ROOT_USER=root
MONGO_ROOT_PASSWORD=mongo_secure_pass

# Port mapping
MONGO_PORT=27017

# ============================================================================
# Application Database Users (created by init scripts)
# ============================================================================

# These users are created automatically by the init scripts
# PostgreSQL admin user (for migrations, schema changes)
DB_ADMIN_USER=air_quality_admin
DB_ADMIN_PASSWORD=admin_secure_password

# PostgreSQL application user (for runtime operations)
DB_APP_USER=air_quality_app
DB_APP_PASSWORD=app_secure_password

# MongoDB application user (created by mongo_init.js)
MONGO_APP_USER=air_quality_user
MONGO_APP_PASSWORD=secure_password

# ============================================================================
# Connection Strings (for application configuration)
# ============================================================================

# PostgreSQL connection strings
DATABASE_URL_ADMIN=postgresql://air_quality_admin:admin_secure_password@localhost:5433/air_quality_db
DATABASE_URL=postgresql://air_quality_app:app_secure_password@localhost:5433/air_quality_db

# MongoDB connection string
NOSQL_URI=mongodb://air_quality_user:secure_password@localhost:27017/
NOSQL_DB_NAME=air_quality_config

# ============================================================================
# Container Configuration
# ============================================================================

# Container names (automatically set by podman-compose)
POSTGRES_CONTAINER_NAME=air-quality-postgis
MONGO_CONTAINER_NAME=air-quality-mongodb

# Network name
NETWORK_NAME=air-quality-network
