# Air Quality Platform - Database Configuration
# Copy this file to .env and update with your actual credentials

# ============================================================================
# DATABASE CONNECTION - ADMIN USER
# Use for: migrations, schema changes, seed data, administrative tasks
# ============================================================================
DATABASE_URL_ADMIN=postgresql://air_quality_admin:admin_secure_password@localhost:5433/air_quality_db
DB_ADMIN_USER=air_quality_admin
DB_ADMIN_PASSWORD=admin_secure_password

# ============================================================================
# DATABASE CONNECTION - APPLICATION USER
# Use for: backend API runtime, ingestion service
# ============================================================================
DATABASE_URL=postgresql://air_quality_app:app_secure_password@localhost:5433/air_quality_db
DB_APP_USER=air_quality_app
DB_APP_PASSWORD=app_secure_password

# ============================================================================
# DATABASE SERVER SETTINGS
# ============================================================================
DB_HOST=localhost
DB_PORT=5433
DB_NAME=air_quality_db

# ============================================================================
# PODMAN/DOCKER CONTAINER SETTINGS (for reference)
# ============================================================================
CONTAINER_NAME=postgis-db
POSTGRES_SUPERUSER=stivel
POSTGRES_SUPERUSER_PASSWORD=stivel

# ============================================================================
# MONGODB CONFIGURATION (NoSQL for user preferences and dashboard configs)
# ============================================================================
MONGO_URI=mongodb://localhost:27017/air_quality_config
MONGO_DB_NAME=air_quality_config
MONGO_HOST=localhost
MONGO_PORT=27017

# For production with authentication:
# MONGO_URI=mongodb://air_quality_app:mongo_app_password@localhost:27017/air_quality_config?authSource=admin
# MONGO_USER=air_quality_app
# MONGO_PASSWORD=mongo_app_password

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. NEVER commit this file to Git (already in .gitignore)
# 2. Change passwords before production deployment
# 3. Use strong, randomly generated passwords in production
# 4. Rotate credentials regularly
# 5. Use different credentials per environment (dev, staging, prod)
# 6. Enable MongoDB authentication in production
# 7. Use TLS/SSL for MongoDB connections in production
